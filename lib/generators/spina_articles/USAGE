Description:
    Generates the migration file required to create the review model

Example:
    rails generate spina_articles:install

    This will create:
        db/migrate/timestamp_create_spina_articles_table.rb
